# Optional. Set this to your custom domain once connected in Vercel.
# If unset, the site URL is derived automatically from Vercel's
# VERCEL_PROJECT_PRODUCTION_URL (the *.vercel.app domain), so deploys
# work with zero configuration. Include the protocol, no trailing slash.
#
# NEXT_PUBLIC_SITE_URL=https://hrantkarapetyan.com
